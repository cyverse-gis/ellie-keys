# create me a heat map of the data

name: heatmap
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - jupyter
  - jupyterlab
  - folium